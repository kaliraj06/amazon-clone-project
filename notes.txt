html data attribute 
-is just an html attribute
-have to start with "data-"
-then give it any name

data set property 
button.dataset.product.name => all datas attached to the element

if two products have same name give seperate id 


const productName = button.dataset.productName;

    let machingItem;
    cart.forEach((item) => {
      if (item.productName === productName) {
        machingItem = item;
      }
    });

    if(machingItem) {
      machingItem.quantity += 1;
    }else{
      cart.push(
      {
        productName: productName,
        quantity: 1
      }


    );

    }
    
  });

});


mvc 
it is a comman software engineering technique

split our code into three parts 
model = save and manages the data 
view = takes the data and displays it on the page
controller = runs some code when we interact with the page
15.00 